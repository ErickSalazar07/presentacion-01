services:
  app:
    build: .
    container_name: go_app
    restart: always
    ports:
      - "8080:8080"
    env_file:
      - ./config/.env
    extra_hosts:
      - "host.docker.internal:host-gateway"
